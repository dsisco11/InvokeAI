import type {
  SelectProps,
  SelectOption,
  CustomChakraStylesConfig,
} from './types';
import Select from './Select';

export type { SelectProps, SelectOption, CustomChakraStylesConfig };
export { Select };
